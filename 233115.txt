13. git fetch origin
git checkout filter-tasks-by-category

15. git add .
git commit -m "Implement filterTasksByCategory method"
git push origin filter-tasks-by-category

16. git checkout -b remove-task-by-name origin/remove-task-by-name
git checkout -b list-tasks-sorted-by-name origin/list-tasks-sorted-by-name
git checkout -b remove-task-by-name origin/remove-task-by-name

git checkout 5-filter-tasks-by-category

git add .
git commit -m "Implement filterTasksByCategory method"
git push origin 5-filter-tasks-by-category

git checkout 1-remove-a-task-by-name
git add .
git commit -m "Implement removeTaskByName method"
git push origin 1-remove-a-task-by-name


git checkout master


git pull origin master


git checkout -b 5-filter-tasks-by-cgit checkout -b 5-filter-tasks-by-category
git checkout -b 1-remove-a-task-by-name
git checkout -b 3-list-tasks-sorted-by-name


22. git checkout master
git merge third-branch-name


23. git push origin master

